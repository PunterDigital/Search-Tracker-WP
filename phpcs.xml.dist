<?xml version="1.0"?>
<ruleset name="WordPress Plugin Minimal Standards">
    <description>A minimal set of code standard rules to check for WordPress plugins.</description>

    <file>.</file>
    <exclude-pattern>/vendor/</exclude-pattern>
    <exclude-pattern>/node_modules/</exclude-pattern>

    <arg value="sp"/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>

    <rule ref="WordPress"/>
</ruleset>